# Examples

Worked samples showing what the system's outputs actually look like — the form before the commitment.

| File | Shows |
|---|---|
| `chapter-analysis-example.md` | A condensed `analyze-chapter` output **with the author's `R:` annotations** — the full curation cycle in miniature |

---

## Where these come from

The excerpts are drawn from a real first-draft manuscript in Brazilian Portuguese (the system's first field test), with the author's permission. The analysis commentary follows the system convention: written in the project's output language — here, Portuguese, since the manuscript is Portuguese.

They were chosen because they demonstrate the three-way interplay that makes the system work:

1. The analysis **quotes literally and suggests concretely**
2. The author **decides item by item** — accepts, alters, rejects, or asks back
3. Rejections become **signatures**; questions get **answers**; the system learns

Note especially the items where the author pushed back ("é uma marca minha" — *it's my signature*) — those decisions flowed into `persona.md` and stopped being flagged in every subsequent analysis. That feedback loop, not the analysis itself, is the product.
